/**
 * AlaiyOS — Role constants (client-side).
 *
 * Loaded first in app_include_js. Mirrors alaiy_os_core/constants/roles.py.
 *
 * ALAIY_OS_ROLES   — roles whose users are confined to the Alaiy OS workspace
 * ALAIY_OS_BYPASS  — roles that bypass all AlaiyOS confinement (full ERPNext)
 * ALAIY_OS_ROUTE   — the workspace route slug
 */
// eslint-disable-next-line no-unused-vars
const ALAIY_OS_ROLES = ["Alaiy OS Manager", "Alaiy OS User"];
// eslint-disable-next-line no-unused-vars
const ALAIY_OS_BYPASS = ["System Manager", "Administrator"];
// eslint-disable-next-line no-unused-vars
const ALAIY_OS_ROUTE = "alaiy-os";
