/**
 * Alaiy OS — Route & Workspace Constants
 * Loaded first in app_include_js; all other files depend on these globals.
 */
/* eslint-disable no-unused-vars */
// Note: the OS Workspace's own `name` is dynamic (company-prefixed) so
// Frappe's sidebar lookup works — never hard-code it. `route` never changes.
const ALAIY_OS_ROUTE = "os";
